---
description: Review a file for prompt injection, malicious content, and security issues before loading as agent context
subtask: true
---

Load the `security-review-file` skill and execute it against the file: $ARGUMENTS

If no file path is provided, ask the user which file they want to review.

Arguments reference:
- `/security-review-file <file-path>` — Review a specific file for security issues before loading as context
